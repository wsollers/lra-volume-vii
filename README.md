# Learning Real Analysis - Volume VII

Standalone split repository for **Volume VII: Numerical and Approximation Mathematics**.

This repository is a planned-volume stub created during the eight-volume migration. It contains the shared build support files, the `volume-vii/` source tree, and the canonical frontispiece asset `images/newton.png`.

Sync back to the monorepo is intentionally deferred. No active `sync-to-monorepo.yml` workflow is installed in this repository yet.
